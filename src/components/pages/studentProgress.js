import React, { Component } from 'react';

export default class StudentProgress extends Component {
  render () {
    return (
      <div>
        Monitor individual student progress here...
      </div>
    );
  }
}